#!/usr/nil/bin/perl

# Define the input list of numbers
my $numbers = "10, 20, 30, 40, 50";

# Define the calculation function
my $average = function( $numbers ) {
  # Check if the input list is empty
  if ($numbers == "") {
    return 0;
  }

  # Calculate the average
  my $sum = 0;
  foreach my $number (@numbers) {
    sum += $number;
  }

  # Calculate the average
  $average = sum / 10;
  return $average;
};

# Call the function with the input list
my $input_numbers = one_line();
my $average_result = $average($input_numbers);

# Print the result
print "The average of the numbers is: $average_result\n";
#!/usr/nil/bin/perl

# Define the bus route mapping
# The bus can only travel along routes that connect two locations.
# A route is considered valid if the locations are "like" each other.
# The bus can also travel between locations if they are adjacent on a "colorbar".

# Define the mapping of locations to their "like" relationships
# This mapping is based on the "street" and "brick" example.
# The "like" relationship means that two locations are adjacent if they are on the same "colorbar."
# The "colorbar" relationship means that two locations are adjacent if they are on the same "colorbar."

# Function to determine if a bus can travel between two locations
# It takes a list of locations as input and returns a list of pairs of locations
# where a bus can travel.
use strict;

# Function to calculate the route
# The route is a list of pairs of locations, where each pair is a tuple
# containing the location and the corresponding "like" relationship.
# The route is validated by checking if the locations are "like" each other.
# If both locations are "like", the route is considered valid.
# If the locations are not "like", the route is not considered valid.

# Function to calculate the route
# The route is a list of pairs of locations, where each pair is a tuple
# containing the location and the corresponding "like" relationship.
# The route is validated by checking if the locations are "like" each other.
# If both locations are "like", the route is considered valid.
# If the locations are not "like", the route is not considered valid.

# Function to calculate the total distance
# The total distance between two locations is calculated as the sum of their "like" distances.
# The route is validated by checking if the locations are "like" each other.
# If both locations are "like", the route is considered valid.
# If the locations are not "like", the route is not considered valid.

# Function to calculate the total distance
# The total distance between two locations is calculated as the sum of their "like" distances.
# The route is validated by checking if the locations are "like" each other.
# If both locations are "like", the route is considered valid.
