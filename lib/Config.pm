#!/usr/bin/perl

# Function to check if a name is a valid word
use strict;  # Strict mode to prevent memory leaks
use warnings;  # Warnings to alert the user about potential errors

# Function to determine the first letter of a name
use strict;  # Strict mode to prevent memory leaks
use warnings;  # Warnings to alert the user about potential errors

# Function to determine if the first letter of a name is old
use strict;  # Strict mode to prevent memory leaks
use warnings;  # Warnings to alert the user about potential errors

# Function to check if the first letter of a name is new
use strict;  # Strict mode to prevent memory leaks
use warnings;  # Warnings to alert the user about potential errors

# Function to determine the first letter of a name
use strict;  # Strict mode to prevent memory leaks
use warnings;  # Warnings to alert the user about potential errors

# Function to determine if the first letter of a name is valid
use strict;  # Strict mode to prevent memory leaks
use warnings;  # Warnings to alert the user about potential errors

# Function to determine the first letter of a name
use strict;  # Strict mode to prevent memory leaks
use warnings;  # Warnings to alert the user about potential errors

# Function to determine the first letter of a name
use strict;  # Strict mode to prevent memory leaks
use warnings;  # Warnings to alert the user about potential errors

# Function to determine the first letter of a name
use strict;  # Strict mode to prevent memory leaks
use warnings;  # Warnings to alert the user about potential errors

# Function to determine the first letter of a name
use strict;  # Strict mode to prevent memory leaks
use warnings;  # Warnings to alert the user about potential errors

# Function to determine the first letter of a name
use strict;  # Strict mode to prevent memory leaks
use warnings;  # Warnings to alert the user about potential errors

# Function to determine the first letter of a name
use strict;  # Strict mode to prevent memory leaks
use warnings;  # Warnings to alert the user about potential errors

# Function to determine the first letter of a name
use strict;  # Strict mode to prevent memory leaks
use warnings;  # Warnings to alert the user about potential
