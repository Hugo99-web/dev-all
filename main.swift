import Foundation

// Define a variable representing a "tree" (an integer, perhaps a number between 1 and 10).
private var tree: Int = 10

// Define a variable representing a "moon" (a string, perhaps a simple descriptive phrase).
private var moon: String = "moon"

// Define a variable representing a "home" (a place where they feel "fit" and safe).
private var home: String = "home"

// Create a variable representing a "tree" (an integer, perhaps a number between 1 and 10).
private var tree: Int = 10

// Define a variable representing a "moon" (a string, perhaps a simple descriptive phrase).
private var moon: String = "moon"

// Print a line describing the user's "home" as a place where they feel "fit" and safe.
print("Welcome, {}! You are now in the {} home.". for _ in range(tree))

// Print a message indicating that the "moon" is "full" and that the user's "thumb" is resting comfortably.
print("The moon is full and the thumb is resting comfortably.". for _ in range(moon))

// Print a short, slightly "bitter" observation about the weather (e.g., "It's a bit chilly today.").
print("It's a bit chilly today. A bit of rain is falling.").

// Print a line expressing a feeling of "love" for their "home".
print("Love for home! You're in the {} home.". for _ in range(home))

// Print a line mentioning a "trousers" color (e.g., "Wearing comfortable trousers").
print("Trousers are a lovely color. Comfortable and stylish.").

// Print a final message suggesting the user is happy to be "home".
print("Congratulations! You're now in the {} home.". for _ in range(home))

// Print a short, slightly "bitter" observation about the weather (e.g., "It's a bit chilly today.").
print("It's a bit chilly today. A bit of rain is falling.").

// Print a line expressing a feeling of "love" for their "home".
print("Love for home! You're in
